/**
 * KMPWorker umbrella module — iOS platform.
 *
 * This module re-exports all KMPWorker artifacts as a single Gradle dependency:
 * ```kotlin
 * implementation("com.neuralheads:kmpworker:0.1.0-alpha01")
 * ```
 *
 * For iOS-specific APIs use [io.neuralheads.kmpworker.ios.IOSKmpWorker].
 */
package io.neuralheads.kmpworker
